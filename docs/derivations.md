# Derivations — what three departed records established, recomputed from public data

Generated by `tools/gen-derivations.mjs` from `/data` as it stood at commit `8ee648a` (2026-08-14). Every figure below is reproducible by anyone with this repository: the rule is stated beside the number, and the number is computed here rather than quoted.

**WHY THIS FILE EXISTS, AND WHY IT IS NOT THREE LEDGER RECORDS.** L-0218, L-0219 and L-0220 asserted counts about this corpus and cited the corpus as their source. On 2026-08-06 the operator ruled that a self-audit does not belong in the scored ledger: the tier ladder grades external documents by their DISTANCE FROM THE EVENT, and a derivation over `/data` is at no distance — it is the corpus restating itself. Under the governing principle, a source is not independent of what it establishes when it IS what it establishes. **The three records left the ledger in that pass and their findings are here instead, which is the only place they were ever checkable.** This file replaces the assertion with a derivation wherever `/data` can carry one, **and says plainly where it cannot.**

## 1. Bare-domain-root citations — and why three different numbers are all correct

A bare root is a citation that names a publisher and retrieves nothing: `https://sansad.in/`. **THE RULE, identical to the one `tools/no-bare-root.mjs` enforces:**

```
isBareRoot(u) = /^https?:\/\/[^/?#]+\/?$/.test(u)
scope: every object carrying a `url` at any depth, in every file under /data except incoming/
```

| unit | count | what it counts |
|---|---:|---|
| citation OCCURRENCES | **288** | every bare-root citation as it appears |
| distinct (record, url) PAIRS | **277** | a record citing one root twice counts once — **this is the gate's unit** |
| distinct ROOTS | **93** | how many different publishers |
| records affected | **231** | |

**The gap between 288 and 277 is entirely duplication** — 11 occurrences on records that cite the same root more than once:

- `L-0074 https://sansad.in/` ×2
- `L-0089 https://cag.gov.in/` ×2
- `L-0097 https://www.sci.gov.in/` ×2
- `L-0105 https://censusindia.gov.in/` ×3
- `L-0132 https://www.amnesty.org/` ×2
- `L-0146 https://jkgad.nic.in/` ×2
- `P-52 https://sansad.in/` ×2
- `P-53 https://cag.gov.in/` ×2
- `P-58 https://cag.gov.in/` ×2
- `P-68 https://www.mospi.gov.in/` ×2

**L-0219 asserts 278 and 92.** The pair count here is 277 and the root count 93. The record was written before citations closed since; `tools/lib/bare-root-allowlist.json` carries the history of what was fixed and when. **A count is not reproducible until its UNIT is stated, and that is the defect this file exists to close** — not the arithmetic, which was never wrong.

## 2. Citations that name no document

L-0219's second partition, and the larger half: a citation that names a genre rather than a document, so no working host would help. **THE RULE:**

```
namesNoDocument(name) = no four-digit year AND no multi-digit number anywhere in the citation name
```

Deliberately generous to the citation — any multi-digit token at all counts as naming something.

- citation occurrences naming no document: **246**
- distinct (record, url, name) triples: **246**
- of those, also a bare root: **138**
- naming no document but NOT a bare root: **108**

**Examples, verbatim from `/data`:**

- `L-0066` — "MSP committee status reporting"
- `L-0067` — "NSS Situation Assessment Surveys and the Dalwai committee framework"
- `L-0068` — "OECD Agricultural Policy Monitoring and Evaluation"
- `L-0069` — "CACP price policy reports and FCI procurement data"
- `L-0070` — "NCRB Accidental Deaths and Suicides in India"
- `L-0071` — "Central Ground Water Board assessment"
- `L-0072` — "Ministry of Agriculture, PMFBY"
- `L-0073` — "Ministry of Agriculture advance estimates"

## 3. WHAT THIS FILE CANNOT DERIVE, and no file over `/data` can

L-0219's other partition — **139 citations returning no document, split 54 refused / 50 under 500 characters of text / 23 without a DNS record / 12 forbidden** — rests on requesting each root through an explicit resolver and measuring the response as text rather than bytes. **That is a retrieval result. No retrieval is stored in this repository**, so the number cannot be recomputed here and is not printed here.

`tools/source-response-check.mjs` measures the same property live and reports it with the artefacts of its own run named. **It is a different object from a derivation: it tells a reader what the hosts do TODAY, not what they did when the record was written.**

The same boundary applies to **L-0218**, whose three unreadable channels are a retrieval finding throughout.

## 4. L-0220's thirteen handover-counted schemes

**THE DISCRIMINATOR, stated as a rule rather than as a conclusion:** the scheme reports a count taken at the moment the state hands something over, and its announced purpose is realised only if a second party then does something further — buys the refill, occupies the house, boards the flight. Where the thing handed over IS the service, there is no further step and no gap.

**The membership is taken verbatim from L-0220's own source field, not reconstructed.** What IS computed is every checkable property of the list, so a reader can test the pattern the record claims.

| record | verdict | title |
|---|---|---|
| L-0012 | `partly` | Goods and Services Tax |
| L-0034 | `partly` | Pradhan Mantri Ujjwala Yojana |
| L-0035 | `partly` | Swachh Bharat Mission |
| L-0036 | `partly` | Jal Jeevan Mission |
| L-0037 | `partly` | Pradhan Mantri Awas Yojana |
| L-0039 | `partly` | Ayushman Bharat PM-JAY |
| L-0044 | `partly` | National highway construction programme |
| L-0048 | `partly` | Dedicated Freight Corridors and the freight modal share |
| L-0050 | `partly` | Household electrification and the supply-hours gap |
| L-0054 | `partly` | UDAN regional connectivity scheme |
| L-0055 | `partly` | Metro rail expansion |
| L-0062 | `partly` | Formalisation: registration against jobs |
| L-0093 | `partly` | UDISE+ moves to individual student records and 1.7 crore enrolments disappear |
| L-0047 *(control)* | `partly` | Railway electrification |
| L-0053 *(control)* | `partly` | Port efficiency |

**The claim: all thirteen carry `partly` and both controls carry `worked`.** Computed: 13 of 13 handover records are `partly`; 0 of 2 controls are `worked`.

**The claim that the scores predate the pattern.** `review/record-history.json`, generated from git by `tools/gen-record-history.mjs`, records every observed change of `assessment`. Of the 15 records above, **2 changed verdict at any point**: L-0047 (worked→partly), L-0053 (worked→partly). A reader can re-derive this with `npm run record-history`.

---

## 5. What a reader loses, stated rather than left to be noticed

A derivation is not a scored record and the difference is not only the verdict. Four things the three records carried have no home here, and pretending otherwise would repeat the fault that removed them:

- **The strongest case on each side.** L-0220 argued, against itself, that measuring at the handover is not laziness — it is the point at which the state’s own action is complete and attributable, counts there are auditable and timely, and several of the thirteen published the assessment that exposed their own gap. A table of thirteen ids does not say that.
- **The declared absences.** L-0220 recorded that it could not establish whether the pattern holds outside this corpus, or whether schemes measure at the handover by choice or because the use measurement does not exist. Those are findings about the limits of the finding.
- **The caveats.** L-0219 recorded that every figure it gave described one client on one day, and named three occasions when that mattered — ten live government hosts wrongly measured as unreachable, the e-Gazette wrongly recorded as unreachable, and two runs of its own partition discarded. L-0218 recorded that its channels finding was a retrieval result throughout.
- **The revisit triggers, and a place in the cross-tabs.** The three no longer appear in any verdict-by-domain or verdict-by-tier count, so a reader comparing this corpus over time will see the ledger fall from 226 to 223 records with no scored record having been withdrawn on its merits.

**What is NOT lost is the arithmetic**, which is the half a reader could never check before: every count above is recomputed from public `/data` with its rule printed beside it, where previously it rested on a private repository returning HTTP 404.

---

**The records this file replaces are gone from the ledger, not overruled.** Nothing here disputes what they found; the change is where a finding of this kind belongs. The test for a future record is whether anything outside this corpus would have to change for the finding to change — if nothing would, it is method.
