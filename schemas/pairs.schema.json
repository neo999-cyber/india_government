{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pairs.schema.json",
  "title": "Paired series — coverage-usage and contested",
  "description": "The fourth data layer. Which series pair with which, which side is which, and what each side should be called are research judgements, not display choices — the same class of knowledge as caveat and unmeasured, both of which were moved out of code for this reason. Built when the trigger set in log cycle 31j fired: a second domain (employment) required absence-pairs.",
  "type": "object",
  "required": [
    "id",
    "kind",
    "domain",
    "a",
    "b",
    "framing"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^PR-\\d{2,3}$",
      "description": "NOT A RENDERED VALUE: the pair id is a routing and React key only. A pair renders inside its host record's page and is identified there by its two side labels and its framing, never by `PR-xx`."
    },
    "kind": {
      "enum": [
        "coverage-usage",
        "contested"
      ],
      "description": "coverage-usage is ASYMMETRIC — a records what was delivered, b what it converted into, and the order is the argument. contested is SYMMETRIC — two instruments measuring the same thing that disagree, where neither answers the other and neither may be shown alone. NOT A RENDERED VALUE: read by `PairSection` to choose between the contested and coverage views and printed by neither. The distinction reaches a reader as two different layouts, which is the view it selects rather than the value itself."
    },
    "domain": {
      "enum": [
        "macro",
        "banking",
        "employment",
        "poverty",
        "human-development",
        "infrastructure",
        "welfare",
        "education",
        "governance",
        "kashmir",
        "federalism",
        "foreign",
        "defence",
        "environment"
      ],
      "description": "The subject area a record belongs to. A TAG SET, not a partition: a ledger record carries `domains[]` and may carry several, a series carries exactly one, and a phase is not a domain — phase 7 produced eight records filed across seven values.\n\nTHE DISTINCTION THAT GETS CONFUSED IS welfare AGAINST human-development: welfare is the DELIVERY of a scheme, human-development is the OUTCOME it was meant to produce. Records carrying both (Swachh Bharat, PM-JAY, the anaemia reversal) are the normal case, not an error — the scheme is one domain and what it did to people is another. Education holds both the delivery and the outcome of schooling; it is not split across welfare and human-development the way a scheme is.\n- macro: aggregate economic conditions and fiscal or monetary policy.\n- banking: banks, credit, financial supervision and resolution.\n- employment: work, labour force, earnings and formality.\n- poverty: measured deprivation and consumption-based poverty.\n- human-development: outcomes in health, nutrition and survival — what delivery produced.\n- infrastructure: physical networks and their capacity or service.\n- welfare: scheme delivery and entitlements — what was provided.\n- education: schooling and higher education — learning, participation, teaching capacity and the education system.\n- governance: institutions of state, enforcement, transparency and rights.\n- kashmir: a cross-cutting lens, applied to records whose primary subject sits elsewhere.\n- federalism: Centre-state relations. Also a lens: GST and groundwater carry it alongside their own subject.\n- foreign: trade, external balances and relations with other states.\n- defence: armed conflict and counter-insurgency operations, external or internal. The state’s treatment of civilians in those operations files governance, not defence.\n- environment: land, water, air and the resource base. NOT A RENDERED VALUE: a filing key, read by `pairsInDomain` to place the pair on a domain surface. The domain reaches a reader as the page the pair was found on, not as a value on the pair."
    },
    "lenses": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "enum": [
          "kashmir",
          "federalism",
          "defence-sector",
          "united-states",
          "china",
          "russia",
          "neighbourhood",
          "europe"
        ]
      },
      "description": "A cross-cutting LENS the record is also read under. `domain` says what the record IS ABOUT; `lenses[]` says what it ALSO BEARS ON. They are two axes, and a single-valued `domain` cannot hold both — every phase-11 series is substantively a defence or governance measurement AND is about Jammu and Kashmir, and while only the first was expressible a reader filtering the instrument on `kashmir` reached no measured spine at all. Optional; absent means no lens applies, which is the ordinary case. A lens is never a substitute for the subject: the substantive subject always goes in `domain`.\n- kashmir: Jammu and Kashmir as place and question — the record's subject sits elsewhere (defence, governance, welfare) and J&K is the territory, population or conflict it is measured over. NEVER a subject value. The domain enum's own line reads \"a cross-cutting lens, applied to records whose primary subject sits elsewhere\", so `domain: \"kashmir\"` declares a lens and no subject at all, and is an error (rule `lens-as-subject`).\n- federalism: Centre-state relations bearing on a record whose subject is elsewhere — GST as a macro record that moves fiscal authority between Centre and states, groundwater as an environment record on a state subject. UNLIKE kashmir this value is ALSO a subject in its own right (\"Centre-state relations\"), so `domain: \"federalism\"` is legal. What is not legal is asserting it on both axes of one record: either alone is correct, both at once says the record's subject is a lens over itself (rule `lens-duplicated`).\n\nSIX VALUES ADDED IN PHASE 14, AND THEY ARE A DIFFERENT SHAPE FROM THE FIRST TWO. `kashmir` and `federalism` were derived FROM the domain enum, which had written both as lenses; these six are not domain values and never will be. A counterparty is not a subject area — \"china\" answers WHO a record is about, not WHAT it is about, and the subject of every one of them is trade, external balances or relations, which is `foreign`. The lens axis is therefore wider than the subset-of-domains relation it happened to have while it held only two values, and `lenses[]` being a subset of the domain enum was an accident of its first population, not a rule.\nA LENS IS ADMITTED WHEN ITS RECORDS LAND, NOT WHEN IT IS PLANNED. `china`, `neighbourhood` and `europe` are named in the phase-14 plan and are deliberately NOT in the enum above: they enter with the records that populate them. A declared value with nothing behind it is a filter that returns nothing to the reader who selects it, which is the same defect as a lens with no page one level in — and the `lens-empty` gate refuses the build rather than trusting an author to notice.\nA COUNTERPARTY LENS IS DECLARED ONLY WHERE THE INSTRUMENT HOLDS A FILE — several records read together because the counterparty is itself the policy object. It is not declared for a single agreement: a lens over one record is a filter that returns what a reader already had. UAE CEPA and Australia ECTA carry no counterparty lens for that reason, and the vocabulary extends when a file forms, not when a country is mentioned.\n- defence-sector: the record's subject is the armed forces, the defence industry or defence procurement, whatever domain it files. DELIBERATELY NOT NAMED `defence`, because the domain of that name is narrower — \"armed conflict and counter-insurgency operations\" — and the lens is wider than it: an acquisition cost files `macro`, an indigenisation share files `foreign`, and neither is armed conflict. Two names for two different extents, so neither can be read as the other.\n- united-states: the United States as counterparty — tariff actions, the negotiation track, visa and services measures, defence and technology frameworks.\n- china: the People's Republic as counterparty — the merchandise deficit and its composition, import dependence and export controls, investment screening, and post-Galwan normalisation READ AS A DIPLOMATIC INSTRUMENT. The conflict itself is phase 11's and files `defence`; this lens is not a route to restating it.\n- russia: the Russian Federation as counterparty — crude and refined-product flows, payment mechanisms, defence supply, and the linkage of any of them into a third country's measures.\n- neighbourhood: the immediate South Asian neighbourhood as ONE object, which is why it is one value and not seven. The counterparty lenses above are each a file because each state is individually a policy object; here the region is the stated policy object — \"Neighbourhood First\" names a region, not a country — and the records are read as a set. A reader wanting one country reads the titles, and if a single neighbour accumulates a file of its own it earns its own value then. Pakistan is a member of the region and the member the doctrine least describes; its records are not yet authored.\n- europe: the EU, the UK and EFTA as counterparties. One value, because the three are one negotiating arc in this instrument's material — an in-force UK agreement, an in-force EFTA agreement carrying an investment commitment, and a live EU negotiation — and splitting them would give each a filter with one record behind it. NOT A RENDERED VALUE: a filing key, read by `pairsUnderLens`. Same as `domain` — it decides where the pair is reachable from and is not printed on it. 29 of 60 pairs declare one."
    },
    "a": {
      "$ref": "#/$defs/side"
    },
    "b": {
      "$ref": "#/$defs/side"
    },
    "framing": {
      "type": "string",
      "minLength": 30,
      "description": "One sentence, specific to this pair. A standing sentence about connections and taps would misdescribe a road network."
    },
    "gapComputable": {
      "type": "boolean",
      "default": false,
      "description": "NOT A RENDERED VALUE: a condition. When false the view renders `gapReason` instead of a computed gap, so what reaches a reader is that field's prose and not this boolean. 56 of 60 pairs declare it false."
    },
    "gapReason": {
      "type": "string",
      "description": "Required when gapComputable is false. Why subtraction is not available — different quantities, different denominators, different populations, or a disagreement rather than a gap."
    },
    "provenanceRefs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^P-\\d{2,3}$",
        "description": "Provenance record id (P-nn) bearing on the comparison this pair makes."
      }
    },
    "notes": {
      "type": "string"
    },
    "title": {
      "type": "string",
      "minLength": 10,
      "description": "Short name for the pair, as authored. The framing is the sentence; this is the handle."
    },
    "ledgerRefs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^L-\\d{4}$",
        "description": "Ledger record id (L-nnnn) whose finding this pair carries."
      },
      "description": "Ledger records this pair bears on. NOT A RENDERED VALUE: and this one is a DEBT rather than a decision. **45 of 60 pairs carry it and nothing anywhere reads it** — not a view, not an accessor, not a gate; its only other appearance in the repository is the type declaration in `lib/types.ts`. Found 2026-08-11 when the pairs non-prose half was brought into `field-render-audit`, which is the first check that could see it. It is exempted rather than rendered because a view added only to satisfy a gate is worse than the gap it closes; what it is FOR — a pair naming the ledger records it bears on — is real and unbuilt."
    },
    "status": {
      "enum": [
        "live",
        "declared-pending"
      ],
      "description": "live (default) means both sides resolve and the pair renders. declared-pending means the pair is recorded as OWED — the research judgement that these two things belong side by side has been made, but at least one side is not yet authored. A pending pair renders nowhere; it is held so the gap is visible as owed rather than silently absent. NOT A RENDERED VALUE: and this one is a DEBT rather than a decision. 14 of 60 pairs carry `declared-pending` or `live` and nothing reads either. Same provenance as `ledgerRefs`: found when the non-prose half entered the audit. CORRECTED 2026-08-11, and the withdrawn sentence read: \"A pending pair is currently indistinguishable from a live one on every surface that shows it.\" That is false and was written one batch before it was tested. A pending pair shows on NO surface: both fail `pairRenders`, and `pairsWithSeriesAsSide` and `pairsHostedOn` each gate on it. The correspondence is exact in both directions - 2 pending, 2 non-rendering, no exceptions - and is enforced by nothing, so the real residual risk is DRIFT rather than misreading."
    }
  },
  "$defs": {
    "side": {
      "type": "object",
      "required": [
        "label"
      ],
      "additionalProperties": false,
      "description": "Exactly one of series, absenceFrom or competingAccountsFrom must be set, unless the pair is declared-pending, in which case a side may carry a label alone.",
      "properties": {
        "series": {
          "type": "string",
          "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
          "description": "Series id occupying this side of the pair. NOT A RENDERED VALUE: resolved by `resolvePairSide` into the Series itself. What reaches a reader is that series' title, chart and points; the id survives only inside the href."
        },
        "absenceFrom": {
          "type": "string",
          "description": "Series or ledger id whose unmeasured declaration occupies this position — the counterpart does not exist and that is the finding. NOT A RENDERED VALUE: resolved into the host record. The reader sees the host's title and a link to it, never the id as a value.",
          "pattern": "^(L-\\d{4}|[a-z0-9]+(-[a-z0-9]+)*)$"
        },
        "absenceIndex": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "Zero-based index into the unmeasured[] array of the record named by absenceFrom. A bare integer declares no contract of its own, so the range it must fall in is stated here: out of range is a dangling reference that resolves. NOT A RENDERED VALUE: selects which entry of the host's `unmeasured[]` occupies this side. The entry renders; the index is the arithmetic that found it."
        },
        "competingAccountsFrom": {
          "type": "string",
          "description": "Provenance id whose competingAccounts occupy this position — the counterpart is a dispute between sources, not a series. NOT A RENDERED VALUE: resolved into the provenance record whose competing accounts occupy this side. The record's accounts render; the id survives only inside the href.",
          "pattern": "^P-\\d{2,3}$"
        },
        "label": {
          "type": "string",
          "minLength": 4,
          "description": "What this side IS. Hand-written per pair and never generic: 'The build record' is not 'sustained use'."
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "gapComputable": {
            "const": false
          }
        }
      },
      "then": {
        "required": [
          "gapReason"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "kind": {
            "const": "contested"
          }
        }
      },
      "then": {
        "properties": {
          "gapComputable": {
            "const": false
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "status": {
            "const": "declared-pending"
          }
        },
        "required": [
          "status"
        ]
      },
      "then": {
        "properties": {
          "gapComputable": {
            "const": false
          }
        }
      }
    }
  ]
}
