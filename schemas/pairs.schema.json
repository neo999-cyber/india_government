{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pairs.schema.json",
  "title": "Paired series — coverage-usage and contested",
  "description": "The fourth data layer. Which series pair with which, which side is which, and what each side should be called are research judgements, not display choices — the same class of knowledge as caveat and unmeasured, both of which were moved out of code for this reason. Built when the trigger set in log cycle 31j fired: a second domain (employment) required absence-pairs.",
  "type": "object",
  "required": [
    "id",
    "kind",
    "domain",
    "a",
    "b",
    "framing"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^PR-\\d{2}$"
    },
    "kind": {
      "enum": [
        "coverage-usage",
        "contested"
      ],
      "description": "coverage-usage is ASYMMETRIC — a records what was delivered, b what it converted into, and the order is the argument. contested is SYMMETRIC — two instruments measuring the same thing that disagree, where neither answers the other and neither may be shown alone."
    },
    "domain": {
      "enum": [
        "macro",
        "banking",
        "employment",
        "poverty",
        "human-development",
        "infrastructure",
        "welfare",
        "education",
        "governance",
        "kashmir",
        "federalism",
        "foreign",
        "defence",
        "environment"
      ],
      "description": "The subject area a record belongs to. A TAG SET, not a partition: a ledger record carries `domains[]` and may carry several, a series carries exactly one, and a phase is not a domain — phase 7 produced eight records filed across seven values.\n\nTHE DISTINCTION THAT GETS CONFUSED IS welfare AGAINST human-development: welfare is the DELIVERY of a scheme, human-development is the OUTCOME it was meant to produce. Records carrying both (Swachh Bharat, PM-JAY, the anaemia reversal) are the normal case, not an error — the scheme is one domain and what it did to people is another. Education holds both the delivery and the outcome of schooling; it is not split across welfare and human-development the way a scheme is.\n- macro: aggregate economic conditions and fiscal or monetary policy.\n- banking: banks, credit, financial supervision and resolution.\n- employment: work, labour force, earnings and formality.\n- poverty: measured deprivation and consumption-based poverty.\n- human-development: outcomes in health, nutrition and survival — what delivery produced.\n- infrastructure: physical networks and their capacity or service.\n- welfare: scheme delivery and entitlements — what was provided.\n- education: schooling and higher education — learning, participation, teaching capacity and the education system.\n- governance: institutions of state, enforcement, transparency and rights.\n- kashmir: a cross-cutting lens, applied to records whose primary subject sits elsewhere.\n- federalism: Centre-state relations. Also a lens: GST and groundwater carry it alongside their own subject.\n- foreign: trade, external balances and relations with other states.\n- defence: armed conflict and counter-insurgency operations, external or internal. The state’s treatment of civilians in those operations files governance, not defence.\n- environment: land, water, air and the resource base."
    },
    "lenses": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "enum": [
          "kashmir",
          "federalism"
        ]
      },
      "description": "A cross-cutting LENS the record is also read under. `domain` says what the record IS ABOUT; `lenses[]` says what it ALSO BEARS ON. They are two axes, and a single-valued `domain` cannot hold both — every phase-11 series is substantively a defence or governance measurement AND is about Jammu and Kashmir, and while only the first was expressible a reader filtering the instrument on `kashmir` reached no measured spine at all. Optional; absent means no lens applies, which is the ordinary case. A lens is never a substitute for the subject: the substantive subject always goes in `domain`.\n- kashmir: Jammu and Kashmir as place and question — the record's subject sits elsewhere (defence, governance, welfare) and J&K is the territory, population or conflict it is measured over. NEVER a subject value. The domain enum's own line reads \"a cross-cutting lens, applied to records whose primary subject sits elsewhere\", so `domain: \"kashmir\"` declares a lens and no subject at all, and is an error (rule `lens-as-subject`).\n- federalism: Centre-state relations bearing on a record whose subject is elsewhere — GST as a macro record that moves fiscal authority between Centre and states, groundwater as an environment record on a state subject. UNLIKE kashmir this value is ALSO a subject in its own right (\"Centre-state relations\"), so `domain: \"federalism\"` is legal. What is not legal is asserting it on both axes of one record: either alone is correct, both at once says the record's subject is a lens over itself (rule `lens-duplicated`)."
    },
    "a": {
      "$ref": "#/$defs/side"
    },
    "b": {
      "$ref": "#/$defs/side"
    },
    "framing": {
      "type": "string",
      "minLength": 30,
      "description": "One sentence, specific to this pair. A standing sentence about connections and taps would misdescribe a road network."
    },
    "gapComputable": {
      "type": "boolean",
      "default": false
    },
    "gapReason": {
      "type": "string",
      "description": "Required when gapComputable is false. Why subtraction is not available — different quantities, different denominators, different populations, or a disagreement rather than a gap."
    },
    "provenanceRefs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^P-\\d{2}$",
        "description": "Provenance record id (P-nn) bearing on the comparison this pair makes."
      }
    },
    "notes": {
      "type": "string"
    },
    "title": {
      "type": "string",
      "minLength": 10,
      "description": "Short name for the pair, as authored. The framing is the sentence; this is the handle."
    },
    "ledgerRefs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^L-\\d{4}$",
        "description": "Ledger record id (L-nnnn) whose finding this pair carries."
      },
      "description": "Ledger records this pair bears on."
    },
    "status": {
      "enum": [
        "live",
        "declared-pending"
      ],
      "description": "live (default) means both sides resolve and the pair renders. declared-pending means the pair is recorded as OWED — the research judgement that these two things belong side by side has been made, but at least one side is not yet authored. A pending pair renders nowhere; it is held so the gap is visible as owed rather than silently absent."
    }
  },
  "$defs": {
    "side": {
      "type": "object",
      "required": [
        "label"
      ],
      "additionalProperties": false,
      "description": "Exactly one of series, absenceFrom or competingAccountsFrom must be set, unless the pair is declared-pending, in which case a side may carry a label alone.",
      "properties": {
        "series": {
          "type": "string",
          "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
          "description": "Series id occupying this side of the pair."
        },
        "absenceFrom": {
          "type": "string",
          "description": "Series or ledger id whose unmeasured declaration occupies this position — the counterpart does not exist and that is the finding.",
          "pattern": "^(L-\\d{4}|[a-z0-9]+(-[a-z0-9]+)*)$"
        },
        "absenceIndex": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "Zero-based index into the unmeasured[] array of the record named by absenceFrom. A bare integer declares no contract of its own, so the range it must fall in is stated here: out of range is a dangling reference that resolves."
        },
        "competingAccountsFrom": {
          "type": "string",
          "description": "Provenance id whose competingAccounts occupy this position — the counterpart is a dispute between sources, not a series.",
          "pattern": "^P-\\d{2}$"
        },
        "label": {
          "type": "string",
          "minLength": 4,
          "description": "What this side IS. Hand-written per pair and never generic: 'The build record' is not 'sustained use'."
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "gapComputable": {
            "const": false
          }
        }
      },
      "then": {
        "required": [
          "gapReason"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "kind": {
            "const": "contested"
          }
        }
      },
      "then": {
        "properties": {
          "gapComputable": {
            "const": false
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "status": {
            "const": "declared-pending"
          }
        },
        "required": [
          "status"
        ]
      },
      "then": {
        "properties": {
          "gapComputable": {
            "const": false
          }
        }
      }
    }
  ]
}
