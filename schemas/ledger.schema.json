{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ledger.schema.json",
  "title": "Reform / event / episode record",
  "type": "object",
  "required": [
    "id",
    "title",
    "date",
    "term",
    "domains",
    "type",
    "summary",
    "assessment",
    "sources",
    "confidence",
    "asOf"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^L-\\d{4}$"
    },
    "title": {
      "type": "string",
      "minLength": 6
    },
    "date": {
      "type": "string",
      "pattern": "^\\d{4}(-\\d{2}(-\\d{2})?)?$",
      "description": "YYYY, YYYY-MM or YYYY-MM-DD; use dateEnd for ranges. Bare YYYY is allowed because some records are known only to the year, and inventing a month would assert precision the source does not have."
    },
    "dateEnd": {
      "type": "string",
      "pattern": "^\\d{4}(-\\d{2}(-\\d{2})?)?$"
    },
    "term": {
      "enum": [
        "baseline",
        "T1",
        "T2",
        "T3"
      ],
      "description": "baseline = pre-May-2014 (UPA era); T1 = 2014-19; T2 = 2019-24; T3 = 2024-"
    },
    "domains": {
      "type": "array",
      "minItems": 1,
      "items": {
        "enum": [
          "macro",
          "banking",
          "employment",
          "poverty",
          "human-development",
          "infrastructure",
          "welfare",
          "education",
          "governance",
          "kashmir",
          "federalism",
          "foreign",
          "defence",
          "environment"
        ],
        "description": "The subject area a record belongs to. A TAG SET, not a partition: a ledger record carries `domains[]` and may carry several, a series carries exactly one, and a phase is not a domain — phase 7 produced eight records filed across seven values.\n\nTHE DISTINCTION THAT GETS CONFUSED IS welfare AGAINST human-development: welfare is the DELIVERY of a scheme, human-development is the OUTCOME it was meant to produce. Records carrying both (Swachh Bharat, PM-JAY, the anaemia reversal) are the normal case, not an error — the scheme is one domain and what it did to people is another. Education holds both the delivery and the outcome of schooling; it is not split across welfare and human-development the way a scheme is.\n- macro: aggregate economic conditions and fiscal or monetary policy.\n- banking: banks, credit, financial supervision and resolution.\n- employment: work, labour force, earnings and formality.\n- poverty: measured deprivation and consumption-based poverty.\n- human-development: outcomes in health, nutrition and survival — what delivery produced.\n- infrastructure: physical networks and their capacity or service.\n- welfare: scheme delivery and entitlements — what was provided.\n- education: schooling and higher education — learning, participation, teaching capacity and the education system.\n- governance: institutions of state, enforcement, transparency and rights.\n- kashmir: a cross-cutting lens, applied to records whose primary subject sits elsewhere.\n- federalism: Centre-state relations. Also a lens: GST and groundwater carry it alongside their own subject.\n- foreign: trade, external balances and relations with other states.\n- defence: armed conflict and counter-insurgency operations, external or internal. The state’s treatment of civilians in those operations files governance, not defence.\n- environment: land, water, air and the resource base."
      }
    },
    "lenses": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "enum": [
          "kashmir",
          "federalism",
          "defence-sector",
          "united-states",
          "china",
          "russia"
        ]
      },
      "description": "A cross-cutting LENS the record is also read under. `domain` says what the record IS ABOUT; `lenses[]` says what it ALSO BEARS ON. They are two axes, and a single-valued `domain` cannot hold both — every phase-11 series is substantively a defence or governance measurement AND is about Jammu and Kashmir, and while only the first was expressible a reader filtering the instrument on `kashmir` reached no measured spine at all. Optional; absent means no lens applies, which is the ordinary case. A lens is never a substitute for the subject: the substantive subject always goes in `domain`.\n- kashmir: Jammu and Kashmir as place and question — the record's subject sits elsewhere (defence, governance, welfare) and J&K is the territory, population or conflict it is measured over. NEVER a subject value. The domain enum's own line reads \"a cross-cutting lens, applied to records whose primary subject sits elsewhere\", so `domain: \"kashmir\"` declares a lens and no subject at all, and is an error (rule `lens-as-subject`).\n- federalism: Centre-state relations bearing on a record whose subject is elsewhere — GST as a macro record that moves fiscal authority between Centre and states, groundwater as an environment record on a state subject. UNLIKE kashmir this value is ALSO a subject in its own right (\"Centre-state relations\"), so `domain: \"federalism\"` is legal. What is not legal is asserting it on both axes of one record: either alone is correct, both at once says the record's subject is a lens over itself (rule `lens-duplicated`).\n\nSIX VALUES ADDED IN PHASE 14, AND THEY ARE A DIFFERENT SHAPE FROM THE FIRST TWO. `kashmir` and `federalism` were derived FROM the domain enum, which had written both as lenses; these six are not domain values and never will be. A counterparty is not a subject area — \"china\" answers WHO a record is about, not WHAT it is about, and the subject of every one of them is trade, external balances or relations, which is `foreign`. The lens axis is therefore wider than the subset-of-domains relation it happened to have while it held only two values, and `lenses[]` being a subset of the domain enum was an accident of its first population, not a rule.\nA LENS IS ADMITTED WHEN ITS RECORDS LAND, NOT WHEN IT IS PLANNED. `china`, `neighbourhood` and `europe` are named in the phase-14 plan and are deliberately NOT in the enum above: they enter with the records that populate them. A declared value with nothing behind it is a filter that returns nothing to the reader who selects it, which is the same defect as a lens with no page one level in — and the `lens-empty` gate refuses the build rather than trusting an author to notice.\nA COUNTERPARTY LENS IS DECLARED ONLY WHERE THE INSTRUMENT HOLDS A FILE — several records read together because the counterparty is itself the policy object. It is not declared for a single agreement: a lens over one record is a filter that returns what a reader already had. UAE CEPA and Australia ECTA carry no counterparty lens for that reason, and the vocabulary extends when a file forms, not when a country is mentioned.\n- defence-sector: the record's subject is the armed forces, the defence industry or defence procurement, whatever domain it files. DELIBERATELY NOT NAMED `defence`, because the domain of that name is narrower — \"armed conflict and counter-insurgency operations\" — and the lens is wider than it: an acquisition cost files `macro`, an indigenisation share files `foreign`, and neither is armed conflict. Two names for two different extents, so neither can be read as the other.\n- united-states: the United States as counterparty — tariff actions, the negotiation track, visa and services measures, defence and technology frameworks.\n- china: the People's Republic as counterparty — the merchandise deficit and its composition, import dependence and export controls, investment screening, and post-Galwan normalisation READ AS A DIPLOMATIC INSTRUMENT. The conflict itself is phase 11's and files `defence`; this lens is not a route to restating it.\n- russia: the Russian Federation as counterparty — crude and refined-product flows, payment mechanisms, defence supply, and the linkage of any of them into a third country's measures."
    },
    "type": {
      "enum": [
        "reform",
        "event",
        "episode",
        "shock",
        "institutional"
      ],
      "description": "What KIND of thing the record is — not what domain it concerns and not how it turned out.\nThe distinction that gets confused is a measure the state chose against a disruption it did not: see the note on `shock`.\n- reform: a measure the state deliberately introduced — a law, scheme, programme or rate change.\n- event: a discrete dated occurrence, over in days or weeks.\n- episode: a pattern or contested question observed over a span, with no single act at its centre.\n- shock: a disruption arriving from outside the government's control.\n- institutional: a change to the rules of the game or to a standing body, rather than a programme run through one.\nUSAGE NOTE, unresolved: `shock` is currently applied both to external disruptions (the taper tantrum, COVID, US tariffs) and to domestically caused failures (the IL&FS collapse; the 2020 migrant exodus, whose own caseAgainst records that the four hours' notice \"was a choice\"). The definition above states the external reading, which fits three of the five. The other two have not been reclassified."
    },
    "summary": {
      "type": "string",
      "minLength": 40
    },
    "claimAtLaunch": {
      "type": "string",
      "description": "What the government said this would achieve, where applicable"
    },
    "whatHappened": {
      "type": "string"
    },
    "assessment": {
      "enum": [
        "worked",
        "partly",
        "failed",
        "reversed",
        "contested",
        "too-early",
        "awaiting-adjudication",
        "no-objective",
        "baseline-context"
      ],
      "description": "What the record concludes. These values had no written definitions until 2026-08-01, which is how `reversed` came to cover two different mechanisms without the type objecting. Definitions:\n- worked: the measure achieved the objective stated at announcement, on the evidence available.\n- partly: the measure achieved part of its stated objective, or achieved it for part of the intended population.\n- failed: the measure did not achieve the objective stated at announcement. Includes a measure struck down by a court, because the outcome is the same and only the mechanism differs — record the mechanism in assessmentNote.\n- reversed: the ENACTING AUTHORITY withdrew or repealed its own measure. `measure` means an INTERVENTION THAT ACTS ON THE WORLD. Withdrawing a disclosure, a publication or a reporting practice is not reversal, even where the authority withdrawing it is the one that established it. Judicial invalidation is NOT reversed either: a court striking a measure down is not the enacting authority changing its mind, and collapsing the two loses the thing that distinguishes a government retreating under pressure from a government overruled. The value attracts anything that ends, which is why this line now states what a measure is and not only who withdrew it.\n- contested: the evidence supports more than one defensible reading and the record does not choose between them.\n- too-early: the measure is in force but has not run long enough for its stated objective to be testable. THE OBSTACLE IS ELAPSED TIME AND THE EVIDENCE TIME ACCUMULATES. Where the obstacle is instead a pending decision by a body outside the enacting authority, the value is awaiting-adjudication. Re-read against its two remaining members on 2026-08-03 and it describes both: L-0061, where outcome data has not yet accrued, and L-0139, the boundary case, where what is awaited is a year of post-commencement orders and any first setting-aside - things that accumulate with time rather than a ruling anyone is waiting on.\n- awaiting-adjudication: the measure is in force and its effect is testable in principle, but the term that would settle the assessment is a pending decision by a body outside the enacting authority. Distinct from too-early, where the obstacle is elapsed time. Added 2026-08-03 after a sweep of all 149 ledger records found five records in this state and only six records carrying too-early at all - so 83 per cent of that value was occupied by a state its own definition did not describe. FOUR of the five take this value: L-0086, L-0127, L-0134 and L-0143. The fifth, L-0139, does NOT, because what it awaits is an act of publication by a holder rather than a decision by an adjudicator; the value is not stretched to absorb the member that contradicts its own definition.\n- baseline-context: pre-2014 context, carried so that post-2014 records are read against a stated starting condition. Never scored; the schema ties it to term \"baseline\".\n- no-objective: the record finds something real, and no objective was stated at announcement against which the finding could be scored. Distinct from contested, which is about the evidence supporting more than one reading. Use where nothing was claimed, not where a claim exists and its outcome is unmeasured — that remains contested (see L-0096). The value asserts nothing about the finding's quality: a no-objective record is as firmly established as any other, and the absence it records is the absence of a claim to test, not of evidence. Roughly half the ledger is in this state — conditions, trends, reporting instruments and structural absences — and before this value existed all of it had to take a value that presupposes an objective, which is why contested became a sink."
    },
    "caseFor": {
      "type": "string",
      "description": "Strongest good-faith case in favour"
    },
    "caseAgainst": {
      "type": "string",
      "description": "Strongest good-faith case against"
    },
    "shockExposure": {
      "type": "string",
      "description": "Which external shocks confound assessment of this record, if any"
    },
    "seriesRefs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
        "description": "Series id whose points this record is read against."
      }
    },
    "provenanceRefs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^P-\\d{2,3}$",
        "description": "Provenance record id (P-nn) whose measurement dispute bears on this record."
      }
    },
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "name",
          "url",
          "tier"
        ],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string"
          },
          "url": {
            "type": "string",
            "format": "uri"
          },
          "tier": {
            "enum": [
              "T1",
              "T2",
              "T3",
              "T4",
              "T5"
            ],
            "description": "Evidence grade of the DOCUMENT ACTUALLY RETRIEVED, not of the institution the subject belongs to. This is the operative rule and the one that goes wrong: a CAG finding known only through a newspaper's account of an RTI reply is T4, because the subject is official but the evidence is relayed. Grade what you hold, not what it is about.\n- T1: official Indian statistical or institutional source, retrieved directly - a ministry release, an RBI, MoSPI or NCRB publication, a CAG report, a Parliamentary reply.\n- T2: multilateral or international statistical source, retrieved directly - World Bank, IMF, ILO, WHO.\n- T3: peer-reviewed research or a working paper.\n- T4: reported or documentary journalism, NGO datasets, and anything RELAYED rather than retrieved - including an official figure known only through a press account, and an RTI reply cited but not obtained.\n- T5: contested composite index. Always carries a dispute record covering its own domain (CLAUDE.md rule 6).\nNote that tier is asserted in exactly one place per layer: on the series itself, or on each entry of a ledger or provenance `sources[]`. A series `source` is a SourceRef and carries NO tier - the object is closed, so a tier inside it is rejected rather than silently ignored."
          }
        }
      }
    },
    "confidence": {
      "enum": [
        "high",
        "medium",
        "low"
      ],
      "description": "How firmly the record's FINDING is established. Distinct from `tier`, which grades the evidence retrieved: a record can rest on a single relayed source and still be high-confidence if what it asserts is not in doubt, and can rest on a primary source and be medium if the reading is contested.\n- high: the finding is well established and would survive a hostile reading.\n- medium: the finding holds but rests on incomplete retrieval or a contested reading.\n- low: the finding is provisional and should not be leaned on.\nUSAGE NOTE, unresolved: the field is ambiguous in practice between confidence in the FINDING and how well RETRIEVED the record is. L-0014 is high on a single T4 source while L-0015 is medium on a T1, which fits the finding reading; L-0089's revisitTrigger ties confidence to retrieving two primary documents, which fits the retrieval reading. The distribution is also heavily skewed — 63 high, 24 medium, 2 low across 89 records — so `low` is close to unattested. Nothing has been reclassified."
    },
    "asOf": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Date this record was last researched/updated"
    },
    "caveat": {
      "type": "string",
      "minLength": 16,
      "description": "A blocking qualification that MUST render wherever this record appears, including compact listings. Use only when the record would mislead without it — an unverified comparability assumption, a circular measure, a disputed instrument. Not for ordinary uncertainty, which belongs in notes. Authored forms range from a short headline flag (\"T5 CONTESTED INDEX\") to a full sentence; the floor exists to stop an empty stub, not to require prose."
    },
    "unmeasured": {
      "type": "array",
      "description": "Dimensions this record SHOULD have but which have no measurement. An absence is a research finding, not an empty cell — declaring it here keeps it visible in every view rather than only where a component happens to supply it. Use when a chain has a missing link, an intended outcome was never measured, or a series stops short of the thing it is cited to demonstrate.",
      "items": {
        "type": "object",
        "required": [
          "what",
          "why"
        ],
        "additionalProperties": false,
        "properties": {
          "what": {
            "type": "string",
            "minLength": 8,
            "description": "The thing that is not measured, stated positively"
          },
          "why": {
            "type": "string",
            "minLength": 12,
            "description": "Why no figure exists — not collected, not published, withheld, or never defined"
          },
          "wouldFill": {
            "type": "string",
            "description": "The source that would close it, if one is identifiable. Doubles as a verification-queue seed."
          },
          "reasonKind": {
            "enum": [
              "not-collected",
              "not-published",
              "withheld",
              "never-defined"
            ],
            "description": "The STATED reason no figure exists — what the responsible body says, not what is true. Where the stated reason is contradicted, set reasonDisputed and carry the contradiction in `why`.\n\nTHE TEST IS WHETHER THE DATA EXISTS, asked in this order:\n- not-collected: never gathered. No record exists to release. If the holder were compelled tomorrow they would have nothing to produce.\n- not-published: exists in a holder's hands, not released. The test is producibility under compulsion, not whether anyone has asked.\n- withheld: exists, release was specifically requested or legally required, and was refused. Narrower than not-published — requires an identifiable refusal, not merely absence of release.\n- never-defined: no agreed definition exists for the quantity, so it could not be collected even in principle. NOT \"nobody has studied it\" — an unstudied but definable quantity is not-collected."
          },
          "reasonDisputed": {
            "type": "boolean",
            "description": "True where the stated reason is contradicted by evidence — for example a body saying data was never maintained while another arm of the same government publishes some of it. The contradiction must be stated in `why`. This is not a fifth reasonKind: it is a dispute about which kind applies."
          },
          "disputeKind": {
            "enum": [
              "evidentiary",
              "normative"
            ],
            "description": "What kind of contradiction reasonDisputed records. Required when reasonDisputed is true.\n- evidentiary: the stated reason is contradicted by evidence that the data exists or was held.\n- normative: the factual claim is not contested; what is contested is the characterisation of the non-release, typically against a legal or judicial obligation."
          }
        },
        "allOf": [
          {
            "if": {
              "properties": {
                "reasonDisputed": {
                  "const": true
                }
              },
              "required": [
                "reasonDisputed"
              ]
            },
            "then": {
              "required": [
                "disputeKind"
              ]
            }
          }
        ]
      }
    },
    "differentFacts": {
      "type": "boolean",
      "description": "True where the two sides of the record do not share a common measure. Three conditions, of which the third is operative: (a) PRECONDITION — caseFor and caseAgainst cite different underlying quantities, measures or events, not the same one weighted differently. (b) PRECONDITION — neither case contradicts the other's factual claim, so granting one side's strongest fact in full leaves the other's case intact. (c) OPERATIVE — no single measure exists, or could be constructed from available data, that places both sides' facts on one ledger. An UNBUILT comparison FAILS (c): if a common denominator exists in principle and merely has not been built, that is a gap in the instrument, not a property of the argument. A counterpart that is DECLARED UNMEASURABLE passes (c). In practice (c) admits only arguments that cross a category boundary — a quantity against a legal ruling, a constitutional process, or a stated absence. Two economic cases almost always share a ledger in principle: an incidence analysis, a disposition table, a water-productivity account. The worked negative is the implicit taxation of farmers, where the OECD Producer Support Estimate nets budgetary support against implicit price transfers by construction."
    },
    "differentFactsNote": {
      "type": "string",
      "minLength": 30,
      "description": "What each side is counting, and why both can be arithmetically correct. Required in practice wherever differentFacts is true, and PERMITTED AND MEANINGFUL ON FALSE — a note on a false flag records why the record is a weighting case rather than a different-facts one, which is the judgement most at risk of being made silently. L-0118 is the worked instance: both sides accept every underlying fact, the temptation is to mark it true, and the note says so. Reading this line as true-only was a real misreading: it made the one deliberate false-note in the corpus look like a stray."
    },
    "assessmentNote": {
      "type": "string",
      "minLength": 20,
      "description": "A note on the assessment VALUE itself — typically that the existing vocabulary does not cleanly fit and the value may change on review. Not a note on the finding, which belongs in caseFor/caseAgainst."
    },
    "revisitTrigger": {
      "type": "string",
      "minLength": 20,
      "description": "A stated condition under which this record should be re-tested. Used where a judgement is correct now but rests on something that will change — most often a common measure that cannot yet be built but will become buildable. Distinct from assessmentNote, which is about the assessment VALUE rather than about when to look again."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "term": {
            "const": "baseline"
          }
        }
      },
      "then": {
        "properties": {
          "assessment": {
            "const": "baseline-context"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "assessment": {
            "enum": [
              "worked",
              "partly",
              "failed",
              "reversed",
              "contested",
              "no-objective",
              "too-early",
              "awaiting-adjudication"
            ]
          }
        }
      },
      "then": {
        "required": [
          "caseFor",
          "caseAgainst"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "differentFacts": {
            "const": true
          }
        },
        "required": [
          "differentFacts"
        ]
      },
      "then": {
        "required": [
          "differentFactsNote"
        ]
      }
    }
  ]
}
