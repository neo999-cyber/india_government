{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ledger.schema.json",
  "title": "Reform / event / episode record",
  "type": "object",
  "required": [
    "id",
    "title",
    "date",
    "term",
    "domains",
    "type",
    "summary",
    "assessment",
    "sources",
    "confidence",
    "asOf"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^L-\\d{4}$"
    },
    "title": {
      "type": "string",
      "minLength": 6
    },
    "date": {
      "type": "string",
      "pattern": "^\\d{4}(-\\d{2}(-\\d{2})?)?$",
      "description": "YYYY, YYYY-MM or YYYY-MM-DD; use dateEnd for ranges. Bare YYYY is allowed because some records are known only to the year, and inventing a month would assert precision the source does not have."
    },
    "dateEnd": {
      "type": "string",
      "pattern": "^\\d{4}(-\\d{2}(-\\d{2})?)?$"
    },
    "term": {
      "enum": [
        "baseline",
        "T1",
        "T2",
        "T3"
      ],
      "description": "baseline = pre-May-2014 (UPA era); T1 = 2014-19; T2 = 2019-24; T3 = 2024-"
    },
    "domains": {
      "type": "array",
      "minItems": 1,
      "items": {
        "enum": [
          "macro",
          "banking",
          "employment",
          "poverty",
          "human-development",
          "infrastructure",
          "welfare",
          "governance",
          "kashmir",
          "federalism",
          "foreign",
          "defence",
          "environment",
          "demography"
        ]
      }
    },
    "type": {
      "enum": [
        "reform",
        "event",
        "episode",
        "shock",
        "institutional"
      ]
    },
    "summary": {
      "type": "string",
      "minLength": 40
    },
    "claimAtLaunch": {
      "type": "string",
      "description": "What the government said this would achieve, where applicable"
    },
    "whatHappened": {
      "type": "string"
    },
    "assessment": {
      "enum": [
        "worked",
        "partly",
        "failed",
        "reversed",
        "contested",
        "too-early",
        "baseline-context"
      ],
      "description": "baseline-context = pre-2014 record carried for context, not scored"
    },
    "caseFor": {
      "type": "string",
      "description": "Strongest good-faith case in favour"
    },
    "caseAgainst": {
      "type": "string",
      "description": "Strongest good-faith case against"
    },
    "shockExposure": {
      "type": "string",
      "description": "Which external shocks confound assessment of this record, if any"
    },
    "seriesRefs": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "provenanceRefs": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "name",
          "url",
          "tier"
        ],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string"
          },
          "url": {
            "type": "string",
            "format": "uri"
          },
          "tier": {
            "enum": [
              "T1",
              "T2",
              "T3",
              "T4",
              "T5"
            ]
          }
        }
      }
    },
    "confidence": {
      "enum": [
        "high",
        "medium",
        "low"
      ]
    },
    "asOf": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Date this record was last researched/updated"
    },
    "caveat": {
      "type": "string",
      "minLength": 16,
      "description": "A blocking qualification that MUST render wherever this record appears, including compact listings. Use only when the record would mislead without it — an unverified comparability assumption, a circular measure, a disputed instrument. Not for ordinary uncertainty, which belongs in notes. Authored forms range from a short headline flag (\"T5 CONTESTED INDEX\") to a full sentence; the floor exists to stop an empty stub, not to require prose."
    },
    "unmeasured": {
      "type": "array",
      "description": "Dimensions this record SHOULD have but which have no measurement. An absence is a research finding, not an empty cell — declaring it here keeps it visible in every view rather than only where a component happens to supply it. Use when a chain has a missing link, an intended outcome was never measured, or a series stops short of the thing it is cited to demonstrate.",
      "items": {
        "type": "object",
        "required": [
          "what",
          "why"
        ],
        "additionalProperties": false,
        "properties": {
          "what": {
            "type": "string",
            "minLength": 8,
            "description": "The thing that is not measured, stated positively"
          },
          "why": {
            "type": "string",
            "minLength": 12,
            "description": "Why no figure exists — not collected, not published, withheld, or never defined"
          },
          "wouldFill": {
            "type": "string",
            "description": "The source that would close it, if one is identifiable. Doubles as a verification-queue seed."
          },
          "reasonKind": {
            "enum": [
              "not-collected",
              "not-published",
              "withheld",
              "never-defined"
            ],
            "description": "The STATED reason no figure exists. Formalises the taxonomy the `why` description already asserts. Records what the responsible body says, not what is true — where the stated reason is contradicted by evidence, set reasonDisputed and carry the contradiction in `why`."
          },
          "reasonDisputed": {
            "type": "boolean",
            "description": "True where the stated reason is contradicted by evidence — for example a body saying data was never maintained while another arm of the same government publishes some of it. The contradiction must be stated in `why`. This is not a fifth reasonKind: it is a dispute about which kind applies."
          }
        }
      }
    },
    "differentFacts": {
      "type": "boolean",
      "description": "True where the two sides of the record are not weighting the same numbers differently but working from different quantities entirely. Distinct from an ordinary contested assessment, where both sides accept the same figures and disagree about what they mean."
    },
    "differentFactsNote": {
      "type": "string",
      "minLength": 30,
      "description": "What each side is counting, and why both can be arithmetically correct. Required in practice wherever differentFacts is true."
    },
    "assessmentNote": {
      "type": "string",
      "minLength": 20,
      "description": "A note on the assessment VALUE itself — typically that the existing vocabulary does not cleanly fit and the value may change on review. Not a note on the finding, which belongs in caseFor/caseAgainst."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "term": {
            "const": "baseline"
          }
        }
      },
      "then": {
        "properties": {
          "assessment": {
            "const": "baseline-context"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "assessment": {
            "enum": [
              "worked",
              "partly",
              "failed",
              "reversed",
              "contested"
            ]
          }
        }
      },
      "then": {
        "required": [
          "caseFor",
          "caseAgainst"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "differentFacts": {
            "const": true
          }
        },
        "required": [
          "differentFacts"
        ]
      },
      "then": {
        "required": [
          "differentFactsNote"
        ]
      }
    }
  ]
}
