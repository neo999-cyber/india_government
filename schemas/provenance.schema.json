{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "provenance.schema.json",
  "title": "Measurement / comparability dispute record",
  "type": "object",
  "required": [
    "id",
    "title",
    "whatChanged",
    "when",
    "affectsDomains",
    "directionOfBias",
    "bridgeExists",
    "sources"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^P-\\d{2}$"
    },
    "title": {
      "type": "string"
    },
    "whatChanged": {
      "type": "string",
      "minLength": 40
    },
    "when": {
      "type": "string"
    },
    "affectsDomains": {
      "type": "array",
      "minItems": 1,
      "items": {
        "enum": [
          "macro",
          "banking",
          "employment",
          "poverty",
          "human-development",
          "infrastructure",
          "welfare",
          "education",
          "governance",
          "kashmir",
          "federalism",
          "foreign",
          "defence",
          "environment",
          "all"
        ],
        "description": "The subject area a record belongs to. A TAG SET, not a partition: a ledger record carries `domains[]` and may carry several, a series carries exactly one, and a phase is not a domain — phase 7 produced eight records filed across seven values.\n\nTHE DISTINCTION THAT GETS CONFUSED IS welfare AGAINST human-development: welfare is the DELIVERY of a scheme, human-development is the OUTCOME it was meant to produce. Records carrying both (Swachh Bharat, PM-JAY, the anaemia reversal) are the normal case, not an error — the scheme is one domain and what it did to people is another. Education holds both the delivery and the outcome of schooling; it is not split across welfare and human-development the way a scheme is.\n- macro: aggregate economic conditions and fiscal or monetary policy.\n- banking: banks, credit, financial supervision and resolution.\n- employment: work, labour force, earnings and formality.\n- poverty: measured deprivation and consumption-based poverty.\n- human-development: outcomes in health, nutrition and survival — what delivery produced.\n- infrastructure: physical networks and their capacity or service.\n- welfare: scheme delivery and entitlements — what was provided.\n- education: schooling and higher education — learning, participation, teaching capacity and the education system.\n- governance: institutions of state, enforcement, transparency and rights.\n- kashmir: a cross-cutting lens, applied to records whose primary subject sits elsewhere.\n- federalism: Centre-state relations. Also a lens: GST and groundwater carry it alongside their own subject.\n- foreign: trade, external balances and relations with other states.\n- defence: armed conflict and counter-insurgency operations, external or internal. The state’s treatment of civilians in those operations files governance, not defence.\n- environment: land, water, air and the resource base.\n- all: the record bears on every domain. Provenance only."
      }
    },
    "affectsSeries": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
        "description": "Series id this dispute reaches. The series must carry this record back, in provenanceRefs or on a break."
      },
      "description": "Series this record is SPECIFICALLY ABOUT — where the finding concerns that series directly. NOT a list of every series the record bears on. The relationship is deliberately asymmetric: a series may declare many provenanceRefs (P-09 and P-14 are declared by nine series each as general WDI methodology), while a record names only the series its finding concerns. Requiring symmetry would make this field an unmaintainable enumeration. The backlink check therefore runs record-to-series only, and that is correct."
    },
    "directionOfBias": {
      "enum": [
        "understates-pre-2014",
        "overstates-pre-2014",
        "understates-post-2014",
        "overstates-post-2014",
        "disputed",
        "obscures",
        "degrades-precision"
      ],
      "description": "What the measurement problem does to the record it bears on.\n\nSTRUCTURAL NOTE, unresolved, and the reason this field is hard to apply: the name asserts a DIRECTION, but only four of the seven values state one. `disputed`, `obscures` and `degrades-precision` state a KIND of problem with no direction attached, and they carry 35 of the 58 records. The field conflates two axes — which way a figure is pushed, and what sort of defect it has. `overstates-pre-2014` has NO users at all. Nothing has been reclassified; the definitions below describe observed use of each value as it stands.\n- understates-pre-2014: the pre-2014 figure was too low, so the earlier period looks better than it was.\n- overstates-pre-2014: the pre-2014 figure was too high. UNATTESTED — no record uses this value.\n- understates-post-2014: the post-2014 figure is too low, suppressing a count that would otherwise show.\n- overstates-post-2014: the post-2014 figure is too high, or flatters by construction.\n- disputed: two parties disagree about the figure or the instrument. No direction is asserted, and the record does not pick.\n- obscures: the figure is missing, withheld or discontinued, so the question cannot be answered either way.\n- degrades-precision: a basis, definition or coverage change breaks comparability across the series, without any claim about direction."
    },
    "bridgeExists": {
      "type": "boolean",
      "description": "Is there any accepted reconciliation across the break?"
    },
    "bridgeNote": {
      "type": "string"
    },
    "competingAccounts": {
      "type": "array",
      "items": {
        "oneOf": [
          {
            "type": "object",
            "required": [
              "holder",
              "position"
            ],
            "additionalProperties": false,
            "properties": {
              "holder": {
                "type": "string"
              },
              "position": {
                "type": "string"
              }
            }
          },
          {
            "type": "string",
            "minLength": 40
          }
        ],
        "description": "Either {holder, position} where the holder separates cleanly, or a plain string where the account names its own holder inside the prose. Both are authored forms; splitting the second by pattern-matching the sentence would be guesswork, and this project does not parse prose to fill fields."
      }
    },
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "name",
          "url",
          "tier"
        ],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string"
          },
          "url": {
            "type": "string",
            "format": "uri"
          },
          "tier": {
            "enum": [
              "T1",
              "T2",
              "T3",
              "T4",
              "T5"
            ],
            "description": "Evidence grade of the DOCUMENT ACTUALLY RETRIEVED, not of the institution the subject belongs to. This is the operative rule and the one that goes wrong: a CAG finding known only through a newspaper's account of an RTI reply is T4, because the subject is official but the evidence is relayed. Grade what you hold, not what it is about.\n- T1: official Indian statistical or institutional source, retrieved directly - a ministry release, an RBI, MoSPI or NCRB publication, a CAG report, a Parliamentary reply.\n- T2: multilateral or international statistical source, retrieved directly - World Bank, IMF, ILO, WHO.\n- T3: peer-reviewed research or a working paper.\n- T4: reported or documentary journalism, NGO datasets, and anything RELAYED rather than retrieved - including an official figure known only through a press account, and an RTI reply cited but not obtained.\n- T5: contested composite index. Always carries a dispute record covering its own domain (CLAUDE.md rule 6).\nNote that tier is asserted in exactly one place per layer: on the series itself, or on each entry of a ledger or provenance `sources[]`. A series `source` is a SourceRef and carries NO tier - the object is closed, so a tier inside it is rejected rather than silently ignored."
          }
        }
      }
    },
    "notes": {
      "type": "string"
    },
    "correctiveSeries": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
        "description": "Series id that corrects for this dispute — the measure to read instead of, or beside, the affected one."
      },
      "description": "Series that reference this record as the CORRECTIVE rather than the affected party — the honest measure the record points readers toward, or the instrument that measures the wedge it describes. A series listed here carries the ref for navigation, not because it is distorted; directionOfBias does not apply to it. Mirror of affectsSeries: a series must not appear in both."
    }
  }
}
